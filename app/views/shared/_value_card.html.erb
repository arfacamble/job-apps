<div class="my-2 p-2 pb-4 flex flex-col border-b border-blue-700 border-opacity-50">
  <div class="mb-2 flex items-center pl-2 pb-2">
    <% if value.image_url %>
      <img src=<%= value.image_url %> class="h-12 w-12 mr-2 rounded-md object-cover object-center">
    <% end %>
    <p class="font-headers text-lg font-light text-background"><%= value.title %></p>
    <% if now_in %>
      <%= link_to 'Remove', application_value_path(app, value), method: :delete, class: 'bg-red-700 text-white py-1 px-2 mx-2 rounded-sm' %>
    <% else %>
      <%= link_to 'Add', application_values_path(app, value_id: value), method: :post, class: 'bg-green-500 text-white py-1 px-2 mx-2 rounded-sm' %>
    <% end %>
  </div>
  <div class="w-5/6 self-end flex border border-red-700 border-opacity-20 rounded-md p-2">
    <p class="font-body italic text-background text-right"><%= value.text %></p>
  </div>
</div>
